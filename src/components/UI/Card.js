import styled from 'styled-components';

const Card = styled.div`
  overflow: hidden;
  box-shadow: 0 0 10px -2px var(--elements-shadow-color);
  border-radius: 5px;
  background-color: var(--elements-bg);
  color: var(--elements-text-color);
`;

export default Card;
